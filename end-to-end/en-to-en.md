# to install virtual python and acativare,and requriments

 ```
conda create -p venv python=3.8 -y
conda activate venv\
pip install -r requirements.txt

 ```

 # to install jubiter notbook
 ```
 pip install ipykernel
 ```